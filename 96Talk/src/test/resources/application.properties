# ADD profile
spring.profiles.active=test

# h2 database (testcase)
spring.datasource.url=jdbc:h2:mem:~/96talk;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=mysql
spring.datasource.username=sa
spring.datasource.password=123123

#Spring Log
logging.level.nineto6.Talk=trace

#MyBatis
mybatis.type-aliases-package=nineto6.Talk.domain
logging.level.nineto6.Talk.mapper.mybatis=trace
mybatis.configuration.map-underscore-to-camel-case=true

# JWT Secret Key
custom.jwt-access-secret-key=accessSecretKey
custom.jwt-refresh-secret-key=refreshSecretKey

# Redis
spring.redis.host=localhost
spring.redis.port=6379